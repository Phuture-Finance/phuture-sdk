overwrite: true
schema: "./schema"
documents: null
generates:
  ./schema/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
