{
  "name": "auto-router",
  "version": "1.0.0",
  "main": "src/index.ts",
  "dependencies": {
    "dotenv": "16.0.3",
    "yesno": "0.4.0",
    "@phuture/sdk": "workspace:*"
  },
  "scripts": {
    "start": "pnpm build && node dist",
    "dev": "tsdx watch",
    "build": "tsdx build"
  }
}
